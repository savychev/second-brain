# Maven
target/

# Локальные данные заметок (не коммитим личные мысли)
data/
*.json.tmp

# Локальные настройки Claude Code (персональные разрешения)
.claude/settings.local.json

# Секреты Notion (токен интеграции). Пример — в notion.properties.example
notion.properties

# Ключи моделей (Groq, Anthropic). Пример — в classifier.properties.example
classifier.properties

# Файлы базы SQLite: там лежат личные заметки, в публичный репозиторий им нельзя.
# -wal и -shm — служебные файлы режима WAL, появляются рядом с базой.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# ОС
.DS_Store
Thumbs.db

# Логи
*.log

# Claude Code local config
.claude/
